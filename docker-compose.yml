# =============================================
# EPCO - Docker Compose
# Servicios: Apache/PHP + MySQL + phpMyAdmin
#
# Los puertos se configuran via .env
# Usar: bash start.sh (auto-detecta puertos libres)
# O manualmente: docker compose up -d
# =============================================

services:
  # =============================================
  # Aplicación PHP/Apache
  # =============================================
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: epco-app
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8090}:80"
    volumes:
      - ./logs:/var/www/html/logs
      - ./public/uploads:/var/www/html/public/uploads
    environment:
      - DB_HOST=db
      - DB_NAME=${DB_NAME:-epco}
      - DB_USER=${DB_USER:-epco_user}
      - DB_PASS=${DB_PASS:-EpcoSecure2026}
      - DB_CHARSET=utf8mb4
      - APP_ENV=${APP_ENV:-production}
      - APP_URL=${APP_URL:-https://soporteti.puertocoquimbo.cl}
      - TZ=America/Santiago
      # SMTP - Configuración de correo
      - SMTP_ENABLED=${SMTP_ENABLED:-true}
      - SMTP_MODE=${SMTP_MODE:-relay}
      - SMTP_HOST=${SMTP_HOST:-smtp.gmail.com}
      - SMTP_PORT=${SMTP_PORT:-587}
      - SMTP_USER=${SMTP_USER:-soporteepco@gmail.com}
      - SMTP_PASS=${SMTP_PASS:-uonfzgkcsfzqxgbc}
      - SMTP_ENCRYPTION=${SMTP_ENCRYPTION:-tls}
      - SMTP_FROM_EMAIL=${SMTP_FROM_EMAIL:-soporteepco@gmail.com}
      - SMTP_FROM_NAME=${SMTP_FROM_NAME:-Soporte TI - EPCO}
      # Relay interno (microservicio de correo)
      - SMTP_RELAY_HOST=mailrelay
      - SMTP_RELAY_PORT=25
      # Destinatarios de notificaciones (separar con coma)
      - NOTIFY_TICKET_CREATED=${NOTIFY_TICKET_CREATED:-soporteepco@gmail.com}
      - NOTIFY_TICKET_UPDATED=${NOTIFY_TICKET_UPDATED:-soporteepco@gmail.com}
    depends_on:
      db:
        condition: service_healthy
      mailrelay:
        condition: service_healthy
    networks:
      - epco-network
      - nginx-proxy-manager

  # =============================================
  # Base de Datos MySQL
  # =============================================
  db:
    build:
      context: .
      dockerfile: Dockerfile.mysql
    image: epco-db:latest
    container_name: epco-db
    restart: unless-stopped
    ports:
      - "${DB_PORT:-3306}:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-EpcoRoot2026}
      MYSQL_DATABASE: ${DB_NAME:-epco}
      MYSQL_USER: ${DB_USER:-epco_user}
      MYSQL_PASSWORD: ${DB_PASS:-EpcoSecure2026}
      TZ: America/Santiago
    volumes:
      - epco-db-data:/var/lib/mysql
    command: >
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
      --default-authentication-plugin=mysql_native_password
    healthcheck:
      test: ["CMD", "bash", "-c", "mysqladmin ping -h localhost -u root -p$${MYSQL_ROOT_PASSWORD} --silent && mysql -u root -p$${MYSQL_ROOT_PASSWORD} -e 'SELECT 1 FROM epco.users LIMIT 1' 2>/dev/null"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 60s
    networks:
      - epco-network

  # =============================================
  # Microservicio de Correo (Postfix Relay)
  # =============================================
  mailrelay:
    build:
      context: .
      dockerfile: Dockerfile.mail
    container_name: epco-mailrelay
    restart: unless-stopped
    environment:
      - RELAY_HOST=${SMTP_HOST:-smtp.gmail.com}
      - RELAY_PORT=${SMTP_PORT:-587}
      - RELAY_USER=${SMTP_USER:-soporteepco@gmail.com}
      - RELAY_PASS=${SMTP_PASS:-uonfzgkcsfzqxgbc}
      - MAIL_HOSTNAME=mail.epco.local
    healthcheck:
      test: ["CMD", "postfix", "status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - epco-network

  # =============================================
  # phpMyAdmin (opcional, para administrar la BD)
  # =============================================
  phpmyadmin:
    image: phpmyadmin:latest
    container_name: epco-phpmyadmin
    restart: unless-stopped
    ports:
      - "${PMA_PORT:-8082}:80"
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-EpcoRoot2026}
      UPLOAD_LIMIT: 64M
    depends_on:
      db:
        condition: service_healthy
    networks:
      - epco-network

# =============================================
# Volúmenes persistentes
# =============================================
volumes:
  epco-db-data:
    driver: local

# =============================================
# Red interna
# =============================================
networks:
  epco-network:
    driver: bridge
  npm_network:
    name: nginx-proxy-manager
    external: true
